group 'com.azukisiromochi.gradient_like_css'
version '1.0'

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.4' // Ensure compatibility with API 35
    }
}

rootProject.allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: 'com.android.library'

android {
    namespace 'com.azukisiromochi.gradient_like_css'
    compileSdk 35 // Updated to Android 16 (API 35)

    defaultConfig {
        minSdk 16
        targetSdk 35 // Updated to Android 16 (API 35)
    }
    lintOptions {
        disable 'InvalidPackage'
    }
}